spring:
  application:
    name: employee-api

  datasource:
    url: jdbc:postgresql://localhost:5433/gerenciamento
    username: admin
    password: admin
    driver-class-name: org.postgresql.Driver

  jpa:
    database-platform: org.hibernate.dialect.PostgreSQLDialect
    show-sql: true

  flyway:
    enabled: true
    locations: classpath:db/migration

employee-api:
  security:
    secret: "3482FOHSDF4324DFSDF23432FDSFADAR27FDDISA"


